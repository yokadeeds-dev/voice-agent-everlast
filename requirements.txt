# Voice Agent – Abhängigkeiten
# Getestet mit Python 3.11

# ── LLM (Groq API – Tool Calling, kostenlos für Demo) ────────────────────────
groq>=0.9.0

# ── STT ───────────────────────────────────────────────────────────────────────
faster-whisper>=1.0.0

# ── TTS (primär) ──────────────────────────────────────────────────────────────
piper-tts>=1.2.0

# ── TTS (Fallback) ────────────────────────────────────────────────────────────
pyttsx3>=2.90

# ── Audio I/O ─────────────────────────────────────────────────────────────────
sounddevice>=0.4.6
scipy>=1.11.0
numpy>=1.24.0

# ── Optional: Non-WAV-Formate (mp3, flac, m4a) ───────────────────────────────
# ffmpeg muss separat installiert sein:
#   Windows: winget install ffmpeg  /  choco install ffmpeg
#   Linux:   apt install ffmpeg
#   Mac:     brew install ffmpeg
